name: Build
description: Build the package

runs:
  using: composite
  steps:
    - name: Setup
      uses: ./.github/actions/setup

    - name: Build the package
      shell: bash
      run: pnpm build
